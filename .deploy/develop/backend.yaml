sipher-chart:
  deploys:
    - name: backend
      replicasCount: 1
      resources:
        requests:
          memory: 512Mi
          cpu: 256m
        limits:
          memory: 512Mi
          cpu: 256m
      services:
        - name: api
          containerPort:
            api: 5500
        # - name: health
        #   type: ClusterIP
        #   containerPort:
        #     health: 5500
        #   health:
        #     live: /api/health
        #     livenessPort: 5500
        #     ready: /api/health
        #     readinessPort: 5500
  env: {}